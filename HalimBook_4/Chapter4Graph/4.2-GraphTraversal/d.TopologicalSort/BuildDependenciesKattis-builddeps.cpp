#include<bits/stdc++.h>

using namespace std;

int main() {
    int n;
    cin >> n;
    char a[15]; char b[15]; char c[15]; char d[15];
    char e[15]; char f[15];

    for (int i = 0 ; i < n;i++){
        scanf("%s: %s %s %s %s %s", &a[0], &b[0],&c[0],&d[0],&e[0],&f[0]);
 
    }
    return 0;
}