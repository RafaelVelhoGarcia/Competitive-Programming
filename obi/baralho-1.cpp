
#include <cstring>
#include <bits/stdc++.h>
using namespace std; 
int C[13];
int U[13];
int E[13];
int P[13];



int main()
{   
    
    char stringu[300];
    
    
    for (int i = 0; i < 13 ; i++)
    {
        C[i] = 0;
        U[i] = 0;
         E[i] = 0;
         P[i] = 0;
        
    }
    
        scanf("%s" , stringu);

        for (int i = 2; i < strlen(stringu), i += 3)
        {
            char letra = stringu[i];

                char cnum[1];
                strcat(cnum,stringu[i-2]);
                strcat(cnum,stringu[i-1]);
                cout << cnum << endl;




            }

            else if var = 'U'



        }


    return 0;
}
